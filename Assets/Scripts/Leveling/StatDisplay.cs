using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class StatDisplay : MonoBehaviour
{
    public Player player;
    public TMP_Text[] statText;
    // Update is called once per frame
    void Update()
    {
        //for (int i = 0; i < StatManager.instance.attributes.Length; i++)
        //{
        //    //statText[i].text = StatManager.instance.attributes[i].value.ModifiedValue.ToString();
        //} 
    }
}
