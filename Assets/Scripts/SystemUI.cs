using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;

public class SystemUI : MonoBehaviour
{
    //public TMP_Text systemName;
    //public TMP_Text factionOwner;

    //private void Update()
    //{
    //    if(systemName.text != GameManager.instance.systemInfo.systemName)
    //    {
    //        systemName.text = GameManager.instance.systemInfo.systemName;
    //    }

    //    if (GameManager.instance.systemInfo.factionInControl && factionOwner.text != GameManager.instance.systemInfo.factionInControl.name)
    //    {
    //        factionOwner.text = GameManager.instance.systemInfo.factionInControl.name;
    //    }
    //}
}
